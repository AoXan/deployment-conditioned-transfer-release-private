# Optional data manifest

Place separately obtained raw inputs under `data/raw/<provider>/`. Record the
provider, version, licence, acquisition date, and checksum in a local copy of the
manifest before attempting any data-dependent reconstruction. Raw files are not
part of this release.
